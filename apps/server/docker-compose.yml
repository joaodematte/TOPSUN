services:
  topsun-api:
    build:
      context: ../..
      dockerfile: apps/server/Dockerfile
    env_file:
      - .env
    environment:
      HOSTNAME: 0.0.0.0
    ports:
      - "8024:${PORT}"
    restart: unless-stopped
    shm_size: "1gb"
