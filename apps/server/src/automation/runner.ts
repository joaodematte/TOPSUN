/**
 * Automation execution is orchestrated by `@topsun/api` inside the TOPSUN monorepo.
 * This module exists so the server process loads automation dependencies at startup.
 */
// oxlint-disable-next-line unicorn/require-module-specifiers
export {};
